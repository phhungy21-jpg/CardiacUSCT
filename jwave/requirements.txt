# Requires python>=3.10
# Toolkit decision (protocol Phase 1.1): jWave, chosen for JAX
# differentiability (future physics-consistency losses / end-to-end learned
# reconstruction). Exact jax/jwave/jaxlib versions to be pinned here once
# Gate 1's reference simulation (jwave/notebooks/phase1_gate1_reference_repro.ipynb)
# has been reproduced and reported back from the target GPU (cloud, per
# jwave/LOG.md run 2026-07-07-01).
numpy==1.24.3
scipy==1.11.1
matplotlib==3.7.2
jwave
jax
jaxlib
